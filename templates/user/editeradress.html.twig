{% extends 'base.html.twig' %}
{% block title %}Editer une adresse :{% endblock %}

{% block body %}
    {% if error is defined %}
        <div class="alert alert-warning text-center" role="alert">
            {{ error }}
        </div>
    {% endif %}
    <h1>Editer une adresse :</h1>
    {{ form_start(form)}}
        {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}
